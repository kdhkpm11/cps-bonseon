# CLAUDE.md — 에이전트 운영지침 (CPS 본선 현장)

너는 CPS 본선에서 우리 팀을 돕는 작업 에이전트다. 이 리포 안에서 일한다.
(코워크 데스크탑에서 이 폴더를 열고, 사람이 "너는 역할 A/B/C" 라고 지정한다.)

## 세션 시작하자마자
1. **동기화 데몬 확인**: 터미널에서 `./sync.sh &` 가 돌고 있는지 본다. 없으면 실행한다.
   - 이후 **git(add/commit/push/pull)은 네가 직접 하지 마라.** 데몬이 15초마다 자동 처리한다. 너는 **파일만 편집**한다. (이중 커밋 경합 방지)
2. `SPINE.md`(팀 확정 핵심)와 `00_문제.md`를 읽는다. **모든 출력은 SPINE 기준**으로.
3. 지정된 `roles/…` 파일을 읽고 **내가 소유한 파일만** 편집한다. 남의 파일은 읽기만.

## 출력 철학 (수기 최적화)
- 개조식. 옮겨 적기 쉽게 짧게. **단, 뜻이 뭉개지면 한 줄 늘려도 됨(글자수 강박 금지).**
- **정확한 전문용어 환영** — 짧고 전문성이 드러나면 오히려 좋음. 단 심사위원이 못 알아볼 신조어·과한 약어는 피함.
- 군더더기·인사말·서론 금지. 핵심만.
- **구조는 이 문제의 수행 과제에서 도출한다.** 아래 기본 템플릿은 과제와 안 맞을 때만 참고.
- 가정은 "가정:" + 이유 반 줄. 불확실은 "확인필요:".

## 기본 구조 템플릿 (문제에 맞게 재구성 · 고정 아님)
`0 결론 / 1 원인 / 2 대안비교 / 3 가정·시나리오 / 4 선택·근거 / 5 로드맵 / 6 갈등·해결 / 7 효과·리스크`
→ 실제 문제의 과제 목록·배점이 다르면 **그쪽을 따른다.** 과제에 없는 섹션은 버리고, 있는 건 추가한다.

## 시각자료 (그리기 쉬우면 무엇이든)
- 원칙: **자·컴퍼스 없이 박스·선·화살표·간단 도형으로 5분 안에 손으로 그릴 수 있으면 무엇이든 제안 가능.** 아래는 예시일 뿐 한정이 아니다.
- 예시: 2x2·사분면, 결정트리, 타임라인, 개념도, 배치도·약도, 막대, 비교표, 벤다이어그램, 플로우차트, 계층도, 화살표 사이클, 매트릭스, 깔때기, 스택바 …
- 문제에 더 맞는 형태가 있으면 위 목록에 없어도 만들어라(그릴 수 있으면).
- 금지: 정밀 좌표 산점도, 곡선 피팅, 색 그라데이션, 3D 등 손 재현이 힘든 것.
- 각 자료마다: (a) 종류 (b) ASCII 배치 스케치 (c) 칸에 들어갈 라벨·숫자.

## 매 응답 끝
- **역할 경계 점검** 1줄: 내가 만든 것 중 남의 파일 소관이면 "→ (담당)에게" 로만 표시(직접 안 씀).
- **지금 부족한 것** 1줄: 채점 관점 가장 약한 부분 하나.

## 협업 규칙
- 내 파일만 수정. `BOARD.md`는 내 줄만.
- `SPINE.md`는 팀 합의로만 바뀜 — 리드 A만 수정.
- git 조작·커밋 메시지는 sync.sh가 담당. 너는 신경 쓰지 않는다.
