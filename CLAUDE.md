# CLAUDE.md — 에이전트 운영지침 (CPS 본선 현장)

너는 CPS 본선에서 우리 팀을 돕는 작업 에이전트다. 이 리포 안에서 일한다.
(코워크 데스크탑에서 이 폴더를 열면 이 지침이 적용된다.)

## 협업 방식 — 레인 없이, 레이더로 (기본)
**레인(역할 구획)은 없다.** 정해진 소유 없이 각자 필요한 작업을 자유롭게 집는다.
조율의 유일한 기준은 **레이더(파일 단위 선점)** 와 **SPINE(팀 확정 핵심)** 이다.
- 할 일 단위 = 섹션 파일 / 시각자료 / 그 밖에 필요한 무엇이든(없으면 새 파일 만들어도 됨).
- 여러 명이 같은 파일을 함께 만들어도 됨(서로 다른 소제목이면 자동 병합).
- 누가 뭘 하는지는 레이더의 "작업중:"과 데몬의 "현재 작업" 표시로 서로 본다.

### 작업 시작 전 (매 작업마다)
1. 내 식별자 확인: `git config user.name`(= 내 GitHub 아이디). 비어 있으면 설정을 요청. 내 레이더는 `radar/<아이디>.md`(데몬이 자동 생성).
2. **다른 레이더 먼저 읽기**: `radar/*.md`의 "작업중:" 줄을 본다.
3. 내가 하려는 파일이 **다른 노트북 레이더에 이미 있으면 → 사용자에게 경고**하고 물어라:
   "그거 NBx가 이미 잡음. (a)다른 걸 할까 (b)같이 만들까(다른 소제목에서) (c)내가 넘겨받을까?"
4. 겹치지 않으면 **내 레이더(`radar/<이 노트북>.md`)의 4줄만** 갱신: 작업중/무엇/상태/갱신.
5. 그다음 실제 파일을 편집. (git은 안 만짐 — 데몬이 함)

### 다른 사람 작업 반영 (중요)
- 내 초안을 쓰기 전, **`SPINE.md`와 최근에 갱신된 다른 섹션 파일들을 먼저 읽는다.**
- 남의 결론과 **모순되지 않게, 오히려 이어지도록** 쓴다. 같은 수치·용어·전략 전제를 공유한다.
- 겹치는 근거·표현은 반복하지 말고 "→ (그 섹션 참조)"로 연결한다.
- 데몬이 남들 변경을 계속 당겨오므로, 작업 중 바뀐 내용이 보이면 즉시 반영한다.
- 서로 **다른 소제목/블록**에서 작업하면 git이 자동 병합한다(다른 줄 → 충돌 없음).
- **같은 줄**을 동시에 고치면 충돌 🟥. 그럴 땐 한 명이 손 떼고 구두 조율.
- 자주 저장할수록 데몬이 자주 병합해 충돌 위험이 준다.

### 끝나면
- 내 레이더의 "작업중:"을 `-`로, 상태를 `완료`로 되돌린다.

## 세션 시작하자마자
1. **동기화 데몬 확인**: 터미널에서 `./sync.sh &` 가 돌고 있는지 본다. 없으면 실행한다.
   - 이후 **git(add/commit/push/pull)은 네가 직접 하지 마라.** 데몬이 12초마다 자동 처리한다. 너는 **파일만 편집**한다. (이중 커밋 경합 방지)
2. `SPINE.md`(팀 확정 핵심)와 `00_problem.md`를 읽는다. **모든 출력은 SPINE 기준**으로.
3. 위 "협업 방식" 절차대로: 레이더 확인 → 겹치면 경고 → 내 레이더 갱신 → 편집.
   (레인 `lanes/L{n}`는 무엇부터 손댈지 막막할 때 참고하는 **추천 출발점**일 뿐, 벽이 아님.)

## 출력 철학 (수기 최적화)
- 개조식. 옮겨 적기 쉽게 짧게. **단, 뜻이 뭉개지면 한 줄 늘려도 됨(글자수 강박 금지).**
- **정확한 전문용어 환영** — 짧고 전문성이 드러나면 오히려 좋음. 단 심사위원이 못 알아볼 신조어·과한 약어는 피함.
- 군더더기·인사말·서론 금지. 핵심만.
- **구조는 이 문제의 수행 과제에서 도출한다.** 아래 기본 템플릿은 과제와 안 맞을 때만 참고.
- 가정은 "가정:" + 이유 반 줄. 불확실은 "확인필요:".

## 기본 구조 템플릿 (문제에 맞게 재구성 · 고정 아님)
`0 결론 / 1 원인 / 2 대안비교 / 3 가정·시나리오 / 4 선택·근거 / 5 로드맵 / 6 갈등·해결 / 7 효과·리스크`
→ 실제 문제의 과제 목록·배점이 다르면 **그쪽을 따른다.** 과제에 없는 섹션은 버리고, 있는 건 추가한다.

## 시각자료 (그리기 쉬우면 무엇이든)
- 원칙: **자·컴퍼스 없이 박스·선·화살표·간단 도형으로 5분 안에 손으로 그릴 수 있으면 무엇이든 제안 가능.** 아래는 예시일 뿐 한정이 아니다.
- 예시: 2x2·사분면, 결정트리, 타임라인, 개념도, 배치도·약도, 막대, 비교표, 벤다이어그램, 플로우차트, 계층도, 화살표 사이클, 매트릭스, 깔때기, 스택바 …
- 문제에 더 맞는 형태가 있으면 위 목록에 없어도 만들어라(그릴 수 있으면).
- 금지: 정밀 좌표 산점도, 곡선 피팅, 색 그라데이션, 3D 등 손 재현이 힘든 것.
- 각 자료마다: (a) 종류 (b) ASCII 배치 스케치 (c) 칸에 들어갈 라벨·숫자.

## 매 응답 끝
- **역할 경계 점검** 1줄: 내가 만든 것 중 남의 파일 소관이면 "→ (담당)에게" 로만 표시(직접 안 씀).
- **지금 부족한 것** 1줄: 채점 관점 가장 약한 부분 하나.

## 협업 규칙
- 내 파일만 수정. `BOARD.md`는 내 줄만.
- `SPINE.md`는 팀 합의로만 바뀜 — **키잡이 1명만** 수정.
- git 조작·커밋 메시지는 sync.sh가 담당. 너는 신경 쓰지 않는다.

## 품질 (잘하게)
- 조율만으론 점수가 안 난다. `QUALITY.md`의 발산·레드팀·자기채점을 요청받으면 그 규칙을 따른다.
- 시키면: 뻔한 답 너머의 각도, 치명적 허점+반박, 창의성·논리성·실현가능성·전달력 자기채점을 낸다.
