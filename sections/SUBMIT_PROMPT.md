# 생성형 AI 활용 내역 (프롬프트 제출용)

> 제17회 전국 창의적 문제해결능력 경진대회 · 고등부 · 「AI 학습코치 하루쌤 2.0 설계」
> 사용 도구: Claude(코워크/Claude Code) · 3인 팀 · GitHub 협업
> 제출 근거: 문제지 "생성형 AI 활용 시 프롬프트 제출" 요구에 대한 답.

---

## 0. AI 활용 원칙 — 사람과 AI의 역할 분담

- AI는 **완성 답안을 대신 쓰지 않음.** 개조식 뼈대·근거 리서치·코드 구현 보조만 맡고, **최종 판단·검증·전사는 사람**이 함.
- 모든 AI 출력은 **사람이 읽고 교정**함(환각 제거·우리 관점 주입). 근거 자료는 **출처를 대조**하고 불확실한 것은 "확인필요"로 표기함.
- 판단 규칙·설계 결정은 사람이 정하고, AI에는 "그 결정을 실증할 근거를 찾아라 / 개조식으로 정리하라 / 코드로 구현하라"를 지시함.

---

## 1. 깃허브 협업 운영 시스템 (기술적 구성)

3인이 같은 GitHub 리포에서 동시에 작업하되 충돌 없이, 방향은 하나로 모으기 위한 자체 운영 체계를 구축함.

### 1-1. 자동 동기화 데몬 (`sync.sh`)
- 각자 노트북에서 `./sync.sh &` 한 번 실행 → 이후 git 명령을 손으로 치지 않음.
- 매 12초: `내 변경 commit → git pull --rebase(남들 것 병합) → git push`.
- 식별자 = **git 계정 이름**(`user.name`)을 파일명 안전형으로 변환 → `radar/<계정>.md` 자동 생성.
- **겹침 경고**: 두 명 이상이 같은 파일을 '작업중'으로 잡으면 🟡, 같은 줄을 동시 수정하면 🟥 경고 + 조율 안내.
- **현재 작업 현황**: 3명이 각각 지금 무슨 파일을 잡고 있는지 상시 표시.

### 1-2. 겹침 방지 레이더 (`radar/<계정>.md`)
- 새 작업 전 자기 레이더의 "작업중:" 한 줄만 갱신 → 데몬이 이를 읽어 겹침을 사전 경고.
- 배타적 파일 소유가 아니라 "선점 표시 + 경고" 방식이라 유연하게 협업함.

### 1-3. 방향 통일 (`SPINE.md`)
- 팀이 합의한 핵심 전략·가정·차별점을 한 곳에 락(lock). **키잡이 1인만 수정.**
- 모든 섹션은 SPINE 기준으로 작성 → 세 사람 작업이 어긋나지 않음.

### 1-4. 병렬 에이전트 리서치 (워크플로우)
- 대규모 근거 수집은 **여러 AI 에이전트를 병렬로 fan-out** → 각자 다른 각도로 검색 → 결과를 **1개 에이전트가 중복 병합·카테고리 정리**하는 워크플로우로 수행함.
- 예: 사례 리서치는 11개 축(프록터링·성적산정·감정인식·취약계층·거버넌스·개인정보 등)을 동시 검색 후 종합. 데이터셋 리서치는 6개 신호군, 카메라 심화는 4개 각도로 병렬 수행.
- API 오류로 단발 에이전트가 끊길 때는 **자동 재시도·캐시 재개(resume)**로 복구함.

### 1-5. 에이전트 운영 지침 (코워크가 폴더를 열면 자동 적용되는 `CLAUDE.md` 요지)
```
너는 우리 팀의 작업 에이전트다. 출력은 완성 원고가 아니라
"그대로 옮겨 적을 개조식 뼈대 + 손으로 그릴 시각자료 설계도"다.
- 개조식, 짧게. 정확한 전문용어 환영, 군더더기 금지.
- 구조는 이 문제의 수행 과제에서 도출한다(고정 템플릿 아님).
- 가정은 "가정:" + 이유 반 줄. 불확실은 "확인필요:".
- 작업 전 radar 확인 → 겹치면 경고 → 내 레이더 갱신 → 편집. git은 데몬이 함.
- 모든 출력은 SPINE 기준으로.
```

---

## 2. 실제 사용한 프롬프트 — 기술 작업

### 2-1. 미션1 (판단 오류 분석) 프롬프트 (요지)
```
민원 10건에서 '판단의 근거가 된 문구'를 지목하고, 그 문구가 드러내는
기술적 결함을 완결된 문장으로 서술하라. 단순 나열 말고 여러 사례에서
반복되는 핵심 판단 오류로 분류하라. 각 진단을 실제 배치사례·선행연구로 검증하라.
분석 틀: Suresh & Guttag(2021) ML 피해 6원천(역사적·표현·측정·집계·평가·배치 편향).
```

### 2-2. 미션2 (하루쌤 2.0 판단체계 설계) 프롬프트 (요지)
```
하루쌤 2.0을 설계하라. 조건: 핵심 정보 3개+ 선택, 미선택 정보 직접사용 금지,
대부분 자동추천(교사 전원검토 불가), 판단보류·교사검토 20% 이하,
단일 신호로 확정판단 금지, 민감정보 수신자별 공개범위 결정, 새 정보 수집만으로 해결 금지.
- 최종 처리는 '규칙(결정 함수)'이 정하고 정보는 '신호'만 준다.
- 위험 신호 앞에선 점수 무관하게 사람에게 넘긴다.
- 모든 추천에 근거 한 줄을 남긴다.
각 설계 결정이 7개 조건 중 어디에 대응하는지 추적표로 매핑하라.
```

### 2-3. 코드 구현 프롬프트 (요지) — `sim/haruel2.py`
```
위 판단 규칙을 부작용 없는 순수 함수(같은 입력→같은 출력)로 구현하라.
ai_hypothesis(학생신호): 정서위기→교사검토(안전 오버라이드), 고성과+피로→감축,
협업→부정행위 자동판정 안 함, 성장추세→상향(라벨 고착 배제).
또래 대규모 데이터는 '참고 의견'으로만 두고 개인 신호·목소리가 우선(오프퀄 안전장치).
판단보류·교사검토가 20%를 넘으면 경계 사례부터 자동추천으로 되돌리되 정서위기는 제외.
```

### 2-4. 병렬 리서치 워크플로우 프롬프트 — 근거 수집 (실제 사용)
공통 배경(문제 개요 + 하루쌤 수집 신호 목록)을 각 에이전트에 주고, 아래처럼 축별로 분담시킴. 각 에이전트는 구조화 스키마(명칭·지역·연도·내용·타당성/위험·하루쌤 함의·출처)로 반환.
```
[각 에이전트 지시 예]
"이 축(예: 원격시험 감독 AI의 편향·피해 / 카메라 기반 참여도 인식 데이터셋 /
접속·과제제출을 feature로 쓰는 학습분석 데이터셋 …)에서 실존·문서화된
사례·선행연구·데이터셋을 4~8건 찾아라. 명칭·연도·출처(URL) 명시,
추측 금지, 불확실하면 '확인필요' 표시. 국내 자료 있으면 포함."

[종합 에이전트 지시]
"수집 목록을 중복 병합하고, 하루쌤 판단오류 패턴(또는 신호)별로 묶어라.
각 카테고리 끝에 '판단체계 시사점' 1~2줄. 표 쓰지 말고 개조식.
맨 끝에 출처 신뢰도 주의 + 국내:해외 비율."
```
- 산출물: `cases.md`(실제 사례 81건→9카테고리), `datasets.md`(핵심신호 데이터셋 47건→예측타당성), `camera.md`(카메라 신호 28건→채택여부 결론), `sim/evidence.md`(설계결정별 근거 라이브러리).

### 2-5. 품질 게이트 프롬프트 (점수 올리기 — 필요 시 사용)
```
- 발산:  이 문제의 서로 다른 접근 6개, 각 한 줄. 뻔한 것 말고 뒤집기·극단화·유추.
- 레드팀: 이 전략의 치명적 허점 3개 + 각 반박. 사정없이 공격.
- 차별화: 심사위원이 기억할 '한 방' 후보 3개(개념 이름/비유/반전).
- 자기채점: 창의성·논리성·실현가능성·전달력 1~5점 + 한 줄 근거. 3점 이하는 개선안까지.
```

---

## 3. AI 활용의 한계와 검증 절차

- 근거 리서치는 AI가 웹에서 모은 것이라 **일부 수치·URL·서지에 오류 가능성**이 있어, 각 파일에 "확인필요" 항목을 명시하고 인용 전 원출처를 사람이 대조함.
- 감정·집중도 판단처럼 **AI가 잘못 쓰면 학생에게 해가 되는 지점**은, 오히려 AI 리서치로 그 위험(편향·타당도 부족·규제)을 실증해 **설계에서 배제·제한하는 근거**로 활용함(카메라 신호 제외 결정).
- 최종 답안 문장·표·시각자료는 **사람이 직접 작성·전사**하며, AI 출력을 검증 없이 그대로 제출하지 않음.

---

> 요약: AI는 (1) 판단체계 설계·코드 구현의 보조, (2) 대규모 근거의 병렬 수집·정리, (3) 3인 협업의 자동 동기화·충돌 방지에 활용되었으며, 모든 결정과 검증의 주체는 사람이었다.

═══════════════════════════════════════════════════════════
# 부록 — 상세 확장 (레포 구조 · 개발 프롬프트 · 실증)
═══════════════════════════════════════════════════════════

## 부록 A. 깃허브 리포 전체 구조 (실제 파일)

리포: `github.com/kdhkpm11/cps-bonseon` (3인 공유, 자동 동기화)

```
cps-bonseon/
├─ 협업 운영 체계
│  ├─ sync.sh                자동 동기화·겹침경고 데몬(계정명 식별, 12초 주기)
│  ├─ radar/<계정>.md        각자 '작업중' 선언 → 겹침 사전 경고
│  ├─ SPINE.md               팀 확정 핵심(전략·가정·차별점) — 키잡이만 수정
│  ├─ CLAUDE.md              코워크 에이전트 자동 운영지침
│  ├─ PLAYBOOK/QUALITY/CONTINGENCY.md   분업·품질게이트·돌발대응
│  └─ LANES.md, lanes/L1~L5.md          작업 묶음(선택적 출발점)
├─ 문제·답안
│  ├─ 00_problem.md          문제 원문
│  ├─ M1_errors.md / M1_verify.md       미션1 판단오류 해체분석·검증
│  ├─ M2_design.md / M2_traceability.md / M2_narrative.md / M2_implementation.md
│  ├─ M3_apply10.md          미션3 10민원 적용
│  ├─ ANSWER_M2_M3.md / ANSWER_M4.md    최종 답안(서술식)
│  ├─ PLAN_M2_M3.md          미션2·3 답안 구조 기획
│  └─ SUBMIT_PROMPT.md       (이 문서) 생성형 AI 활용 내역
├─ 근거 라이브러리 (병렬 리서치 산출)
│  ├─ cases.md               실제 오판 사례 81건 → 9카테고리
│  ├─ datasets.md            핵심신호 데이터셋 47건 → 예측 타당성
│  ├─ camera.md              카메라 신호 28건 → 채택여부 결론
│  └─ sim/evidence.md        설계결정별 근거 라이브러리(316줄)
└─ 실증 코드·플랫폼
   ├─ sim/haruel2.py         판단 규칙 엔진(순수 함수 결정)
   ├─ sim/haruel2_bigdata.py 빅데이터 종합(6천명·백분위·conformal·kNN·Ofqual가드)
   ├─ sim/negotiate.py       학생-AI 다중 턴 협상(타협점 수렴)
   └─ sim/stress_test.py     가상 학생 약 200만 명 대량 통계 검증
```
(웹 학습 플랫폼은 별도 리포에서 fly.io로 배포: https://haruel2-demo.fly.dev/)

## 부록 B. 실증 산출물 — 작동하는 엔진과 학습 플랫폼

설계를 문서로 두지 않고 실제로 돌아가는 코드로 구현함.
- **판단 엔진**(`sim/haruel2.py`): `decide()`가 학생 신호를 받아 자동추천/판단보류/교사검토를 반환. 순수 함수라 같은 입력이면 같은 결과.
- **빅데이터 종합**(`sim/haruel2_bigdata.py`): 6천 명 참조집단에서 백분위 임계값·conformal 20% 예산·kNN 또래 2차소견을 계산하고, 오프퀄 가드로 개인 우선을 강제.
- **학습 플랫폼**(Flask, fly.io 배포): 학생 페르소나 로그인 → 문제 풀이(신호 갱신→추천 변화) → 하루쌤과 대화(협의) → 학습 리포트(6소견 패널) → 개인정보·거버넌스 페이지 → 교사 뷰(감정 원문 잠금·상담 권장). 라우트: `/ /home /learn /chat /report /privacy /teacher /about /feel /api/decide`.

## 부록 C. 개발에 실제 사용한 병렬 에이전트 프롬프트 (요지)

웹 플랫폼은 병렬 에이전트로 빌드함. 실제로 준 지시의 요지는 다음과 같음.

```
[웹앱 빌드 에이전트]
심사위원이 학생처럼 직접 조작하며 하루쌤 2.0의 작동을 체험하는 학습 플랫폼을
Flask로 만들어라. engine/*.py(판단 엔진)를 그대로 임포트해 재사용하고,
로그인(페르소나)→홈(오늘의 추천+정답률 SVG 그래프)→문제풀이(풀면 추천이 실제로 바뀜)
→대화(버튼으로 목표·맥락 선언 시 추천 조정)→교사 뷰(거버넌스 반영)를 구현하라.
외부 CDN 금지, 세션으로 학생 상태 유지, 반드시 로컬 기동해 검증하라.

[UX·구조 고도화 에이전트]
표현 계층만 손대(엔진·라우트·세션 불변) 실제 에듀테크처럼 다듬어라.
디자인 시스템 토큰화 + 다크모드, 온보딩, 인라인 SVG 그래프/아이콘, 모바일 반응형.
구조적 깊이: 개념별 숙달도, 학습 리포트(/report, 6소견 패널),
개인정보·거버넌스(/privacy), 세션 추천 이력 누적. 로컬 검증 필수.
```

## 부록 D. 제품 속 생성형 AI 프롬프트 (판단자가 아니라 신호 생성기)

배포된 플랫폼 안에서 LLM(Claude)은 두 역할만 하며, 최종 판단은 규칙 엔진이 유지함. 실제 시스템 프롬프트:

```
[감정 신호 분류 — 자유서술 자기진단을 신호로]
"너는 학생의 자기진단 문장을 읽고 정서 상태를 분류한다.
 반드시 crisis, tired, ok 중 한 단어만 출력한다.
 crisis=자해·심각한 무력감·위기, tired=피로·지침·부담, ok=그 외."
→ 이 신호를 받아 판단은 규칙 엔진(decide)이 함. 키 없으면 규칙 기반으로 폴백.

[말투 전달 — 규칙의 판단을 학생 말투로]
"너는 학습 코치 '하루쌤'이다. 규칙 엔진이 이미 정한 판단이 주어진다.
 판단을 절대 바꾸지 말고, 결정을 학생에게 따뜻하고 짧게(2문장 이내) 전한다.
 새로운 추천을 지어내지 마라."
```
설계 원칙과 일치: AI는 신호를 만들 뿐 판단자가 아니며, 이는 문제의 "AI가 어떤 조건에서 도움/해가 되는지 구분"에 대한 답이다.

## 부록 E. 대량 통계 검증 프롬프트와 실측

```
[대량 검증 지시]
정답률·추세·오답반복·제출률·정서·목표/맥락선언·협업·회피를 무작위 조합해
가상 학생 약 200만 명을 생성하고 엔진에 통과시켜라. 지어낸 수치 금지, 실측만 보고.
측정: 안전 누락(위기 미이관)·환경 프록시 불이익·명확사례 일치율·20% 예산·종합 방어율.
100%가 아니면 잔여를 정직하게 설명하라.
```
실행 결과(199만 9,999건, 실측): 안전 누락 0건 · 환경 프록시 불이익 0건 · 명확 사례 일치율 100% · 판단보류+교사검토 6.68%(20% 이내) · **종합 방어율 99.81%**. 잔여 0.19%는 학생이 스스로 기초를 택한 협의 결과가 대부분.

## 부록 F. 답안 산출·품질 개선에 쓴 프롬프트 (요지)

```
[근거 요구]  결론만 나열 금지. 모든 핵심 주장에 근거(수치·인과·자료 인용)를 붙여라.
[서술식 개선]  개조식을 완결된 문장의 서술식으로. 엠대시·기호 남용 금지. 표는 유지.
[조건 추적]  각 산출물에 설계 조건 C1~C7이 어디서 반영됐는지 (C2)처럼 명시하라.
[미션3 적용]  미션2 결정 규칙을 예외 없이 그대로 10민원에 적용하고, 각 학생에
             반영된 조건·원칙을 표의 열로 명시하라(원칙↔적용 일치).
```

> 최종 정리: 우리는 생성형 AI를 설계 보조·병렬 근거 수집·코드 구현·협업 자동화, 그리고 배포된 제품 안의 '신호 생성기'로 활용했다. 판단과 검증, 손글씨 전사의 주체는 언제나 사람이었고, AI가 잘못 쓰이면 해가 되는 지점(카메라·감정 오판)은 오히려 배제·제한의 근거로 삼았다.
