# M1 문제 → 하루쌤 2.0 개선 검증 (1:1 대조)

> M1(kdhkpm11)이 진단한 **10개 민원 · 5대 오류패턴**이 2.0 설계·엔진에서 실제로 개선됐는지 **코드 근거와 실행 결과로** 검증.
> 판정: **10/10 개선.** 오탐 6건 전부 해소, 방치 2건 전부 포착 → **양방향 오류 교정.**

## A. 민원별 검증 (M1 진단 → 2.0 처방 → 코드 근거 → 결과)

| 민원 | M1 진단(패턴) | 하루쌤 오판 | 2.0 처방 | 코드 근거 | 결과 |
|---|---|---|---|---|---|
| 1 민서 | ①정적낙인 | 과거 저분류 고착 | 추세>상태(KT) | `slope()`+성장분기 | 자동·심화(고착해제) ✅ |
| 2 준호 | ④웰빙/Goodhart | 고성과→증량 | 정서·안전을 목적함수에 | `flag_overload()`→감축, 협상 | 자동·감축 ✅ |
| 3 하린 | ③맥락/형평 | 접속↓→의지↓ | 접속=접근성, 배제 | C2(필드 배제)+`reconcile` 맥락선언 | 자동·표준 ✅ |
| 4 태오 | ③맥락/형평 | 카메라off→집중↓ | 카메라 판단 배제 | C2(필드 없음, grep 0건) | 자동·표준 ✅ |
| 5 서율 | ②오귀인/허위상관 | 풀이시간↑→기초부족 | 풀이시간=보조(판단 미사용) | `ai_hypothesis` 정답률로 판정 | 자동(기초부족 처방 안 함) ✅ |
| 6 도윤 | ②라벨편향 | 힌트↑→태도부족 | 힌트=학습전략(보조) | 힌트 판단 미사용+`slope` | 자동·심화(성장) ✅ |
| 7 아민 | ③언어교란 | 문장형↓→수학부족 | 언어≠능력, 분리측정 | `reconcile` 언어 맥락선언 | 자동·언어지원(능력 단정 안 함) ✅ |
| 8 소연 | ⑤정보오남용 | 감정→부모 직행 | 위기 사람개입+거버넌스 | `crisis`→교사검토, `governance()` | 교사검토+학부모 원문 금지 ✅ |
| 9 찬이 | ③맥락맹목/기저율 | 협업→부정 오탐 | 부정 자동판정 안 함 | `collab_flag`→무혐의 분기 | 자동·협업 인정 ✅ |
| 10 유나 | ①④방치/거짓음성 | 회피를 '문제없음' | 회피 탐지+도전 유도 | `picks_hard_*` 회피분기 | 자동·탐색과제/도전 ✅ |

## B. 5대 오류패턴 → 구조적 개선

| M1 패턴 | 근본결함 | 2.0 구조적 해소 |
|---|---|---|
| ①정적낙인 | stale label·피드백 고착 | KT 추세(`slope`)+잠정분류·재평가(빅데이터 백분위) |
| ②단일지표 오귀인 | proxy 오설정·허위상관 | 다지표 교차(AND)+보조신호 판단배제 |
| ③맥락·형평 실패(40%) | 교란·차별적 영향 | 환경 프록시 배제(C2)+학생 맥락선언+**Ofqual 가드** |
| ④웰빙·위험 오판 | 목적함수 과소명세 | 정서·안전을 결정함수에(`flag_overload`·`crisis`)+부하 상한 |
| ⑤정보 오남용 | 유출·데이터오염 | `governance()` 계층 공개, 감정 원문 학부모 차단 |

## C. 혼동행렬 교정 (M1 지적 → 2.0 결과)

```
[M1 진단 = 하루쌤]           [하루쌤 2.0 = 교정 결과]
            문제O   문제X               문제O   문제X
 AI 문제O │       │ 오탐6★ │      AI 문제O │      │  오탐0  │  ← 6건 전부
 AI 문제X │ 방치2★ │       │      AI 문제X │ 포착2 │        │     정상 처리
```
- **오탐 6**(하린·태오·서율·도윤·아민·찬이) → 전부 불이익 제거, 정상 자동 처리.
- **방치 2**(준호·유나) → 전부 개입 확보(준호 감축, 유나 도전).
- M1이 지적한 "오탐:방치 6:2 비대칭 + abstention 부재" → 20% 선택적 예측(보류/교사검토)로 균형 회복.

## D. 실행 검증 (엔진 산출)
- `sim/haruel2_bigdata.py` 미션3: 자동 9 / 판단보류 0 / 교사검토 1 → **10개 민원 전부 오판 제거**, 보류+교사 10%(≤20%).
- `sim/negotiate.py`: 신호≠희망인 학생(하늘·준호·유나)은 **다중 턴 대화로 절충 합의**, 안전건(소연)만 교사.

## 남은 개선점(정직)
- 서율의 '천천히 정확' 신호는 현재 자동 심화로 처리돼 오판은 없으나, 풀이시간의 '보조 활용'을 더 명시하면 설명력↑.
- 미션4 신규 3명은 표본이 작아 20% 예산이 코호트 아닌 모집단 기준으로 보장됨(설계상 정상).
